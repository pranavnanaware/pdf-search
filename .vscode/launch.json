{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debug PDF Worker",
      "skipFiles": ["<node_internals>/**"],
      "program": "${workspaceFolder}/app/workers/pdf-worker.ts",
      "outFiles": ["${workspaceFolder}/app/workers/pdf-worker.js"],
      "preLaunchTask": "build:workers"
    }
  ]
} 